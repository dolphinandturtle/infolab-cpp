#include <stdint.h>

uint64_t str_lenght(uint8_t* str);
uint64_t ascii_to_int(const uint8_t ascii);
uint64_t str_to_int(const uint64_t count, uint8_t str[count]);
double str_to_double(const uint64_t count, uint8_t str[count]);
